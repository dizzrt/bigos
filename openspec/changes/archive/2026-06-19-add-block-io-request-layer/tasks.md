## 1. 请求层基础

- [x] 1.1 梳理现有块设备、设备框架和 page/buffer cache 调用点，确认普通消费者与底层执行接口的边界
- [x] 1.2 新增块 I/O 请求描述、操作类型、少量请求层专用状态和按设备固定容量队列定义，保持 freestanding-safe 和固定内存边界
- [x] 1.3 实现请求校验，覆盖目标设备、操作类型、扇区范围、缓冲区长度和算术溢出
- [x] 1.4 实现同步提交路径，完成按设备入队、顺序 dispatch、完成状态记录和单设备队列满确定性失败

## 2. 块设备执行接口对接

- [x] 2.1 将请求 dispatch 对接到设备框架发布的 `BlockDevice` 读写入口，保持现有扇区大小和错误状态语义
- [x] 2.2 保留底层块设备读写 API 作为请求层执行接口，并限制普通存储消费者继续直接调用
- [x] 2.3 覆盖只读设备写入、设备未发布、请求无效、单设备队列满、后端超时/错误和 unsupported 写入的状态传播
- [x] 2.4 审查初始化顺序，确保块设备发布完成后再让 cache 和文件系统提交请求

## 3. page/buffer cache 迁移

- [x] 3.1 将 cache 读未命中装入改为通过块 I/O 请求层提交同步读请求
- [x] 3.2 将 dirty block 显式写回、`fsync`/`sync` 和设备范围同步改为通过请求层提交同步写请求
- [x] 3.3 将 dirty victim 淘汰写回改为复用请求层失败语义，失败时保留原 device/block key 和 dirty/pending 状态
- [x] 3.4 确认 persistent `/rw` clean-sync 成功只在相关请求和底层写入成功后发布

## 4. 上下文与失败边界

- [x] 4.1 为请求提交和 cache 装入/写回加入普通可阻塞内核上下文约束或诊断路径
- [x] 4.2 审查 IRQ、timer、scheduler critical section 和 preemption-disabled 路径，确保不会提交阻塞块 I/O 请求
- [x] 4.3 确认队列容量耗尽、请求校验失败和设备错误不会 panic、死等或伪造持久成功
- [x] 4.4 明确当前仅同步完成，避免暴露 async callback、后台 worker 或用户可见 ABI 承诺

## 5. 验证与文档

- [x] 5.1 增加或更新默认关闭的请求层验证，覆盖成功同步读写、非法请求、队列满和设备错误传播
- [x] 5.2 增加或更新 cache/persistent `/rw` 验证，覆盖写回失败保留 dirty、dirty victim 失败不复用和 clean-sync 成功边界
- [x] 5.3 运行最窄可用的 `xmake` 构建；如本地缺少 `x86_64-elf` 工具链，记录缺失工具和残余风险
- [x] 5.4 对新增或修改的 C++ 源/头运行 clang/clangd 辅助检查，区分历史诊断、当前变更诊断和 freestanding 配置差异
- [x] 5.5 在 QEMU 或 Bochs 可用时运行相关默认关闭 smoke；如模拟器、ROM/显示或磁盘镜像不可用，记录跳过原因
- [x] 5.6 更新必要的文档或验证记录，说明该能力是有界同步请求层，不声明 async I/O、新存储后端、SMP I/O 或完整块设备模型
